# i18nファイル分割計画

**作成日**: 2025-11-27  
**状態**: 🟡 実装中  
**優先度**: 高  
**問題**: 現在の`lib/i18n/index.ts`が4000行を超え、メモリ圧迫が発生

---

## 📋 現状の問題

- `lib/i18n/index.ts`が4347行
- TranslationKey型定義: 約1430行
- en辞書: 約1430行
- ja辞書: 約1430行
- メモリ圧迫が発生し始めている

---

## 💡 分割方針

### Phase 1: 型定義の抽出 ✅
- `lib/i18n/types.ts`に`TranslationKey`型と`Dictionary`型を移動
- 完了済み

### Phase 2: 言語ファイルの分割
- `lib/i18n/en.ts` - 英語辞書
- `lib/i18n/ja.ts` - 日本語辞書
- 各ファイルは`Dictionary`型を実装

### Phase 3: index.tsのリファクタリング
- 言語ファイルをインポート
- `t()`関数はそのまま維持（同期版）
- 将来的に動的インポートに対応可能な構造に

### Phase 4 (将来): 動的インポート対応
- 必要になった時だけ言語ファイルを読み込む
- バンドルサイズ削減と初期ロード速度向上

---

## 📁 新しいディレクトリ構成

```
lib/i18n/
├── types.ts          # TranslationKey, Dictionary型定義
├── en.ts            # 英語辞書
├── ja.ts            # 日本語辞書
├── index.ts         # t()関数、言語選択ロジック
└── storage.ts       # 既存のストレージ関連（変更なし）
```

---

## 🔄 移行手順

1. ✅ `types.ts`を作成（完了）
2. `index.ts`から`en`辞書を抽出して`en.ts`に移動
3. `index.ts`から`ja`辞書を抽出して`ja.ts`に移動
4. `index.ts`をリファクタリングして、分割されたファイルをインポート
5. ビルドエラーがないことを確認
6. 動作確認

---

## ⚠️ 注意事項

- `t()`関数は現在同期的に使われているため、まずは同期版で分割
- 動的インポートは後から追加可能な構造にする
- 型安全性を保つため、`Dictionary`型を使用

---

## 📝 実装済み

- ✅ `lib/i18n/types.ts` - 型定義ファイル作成（1434行）
- ✅ `lib/i18n/en.ts` - 英語辞書ファイル作成（1438行）
- ✅ `lib/i18n/ja.ts` - 日本語辞書ファイル作成（1438行）
- ✅ `lib/i18n/index.ts` - リファクタリング完了（70行、元4347行から大幅削減）
- ✅ ビルド成功確認

---

## ✅ 完了

分割が完了し、ビルドも成功しています。

**改善効果:**
- `index.ts`: 4347行 → 70行（98.4%削減！）
- メモリ圧迫の解消
- 保守性の大幅向上
- 将来の動的インポート対応が可能な構造

