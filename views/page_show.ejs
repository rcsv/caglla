<!DOCTYPE html>
<html>
<head>
  <title><%= page.title %></title>
</head>
<body>
  <p><a href="/albums/<%= album.id %>">Back</a> | <a href="/logout">Logout</a></p>
  <h1><%= page.title %></h1>
  <p><%= page.content %></p>
  <a href="/albums/<%= album.id %>/pages/<%= page.id %>/edit">Edit</a>
  <form action="/albums/<%= album.id %>/pages/<%= page.id %>/delete" method="post" style="display:inline">
    <button type="submit">Delete</button>
  </form>
</body>
</html>
